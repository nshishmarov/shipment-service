package database

import (
	// "service/src/database/interfaces"

	// "gorm.io/driver/postgres"
	// "gorm.io/gorm"
)

// type SqlHandler struct {
// 	db *gorm.DB
// }

func NewSqlHandler() {
	// dsn := "host=localhost user=postgres password=postgres dbname=postgres_db port-5432 sslmode=disabled TimeZone=Asia/Shanghai"
	//db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{})
	// if err != nil {
	// 	panic(err.Error)
	// }
}